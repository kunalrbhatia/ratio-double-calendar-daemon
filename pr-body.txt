### Description

Corrects the mapping of SENSEX `BFO` (BSE F&O) tokens from `exchangeType: 4` (which is BSE Equity) to `exchangeType: 5` (which is BFO) according to the official Angel One SmartAPI specifications.

### Changes

- Changed `exchangeType` to `5` for `BFO` tokens inside `src/execution/smartStream.ts`.
- Updated test cases in `__tests__/smartStream.test.ts` to expect `exchangeType: 5`.

### Verification

- Ran `pnpm test` successfully. All test suites pass with 100% code coverage.
