Repository: https://github.com/kunalrbhatia/ratio-double-calendar-daemon

TASK: Change the risk/P&L monitoring start time from 09:15 IST to 09:30 IST.

CONTEXT:
This is a Node.js/TypeScript options trading daemon (Ratio Double Calendar Spread on 
NIFTY/SENSEX via Angel One SmartAPI). Right after market open (~09:15-09:30 IST), 
option premiums swing sharply due to opening volatility/price discovery. Starting 
the monitoring loop at 09:15 causes the daemon to evaluate stoploss/profit-target 
P&L against noisy, unreliable prices during this window. Delaying monitoring start 
to 09:30 IST avoids acting on these early false signals.

STEPS:
1. Search the codebase (particularly src/scheduler/, src/risk/, src/execution/, 
   and any node-cron expressions or Day.js/dayjs time constants) for anywhere 
   09:15, "9:15", "09 15", or a cron expression equivalent to 15 9 * * * is used 
   to gate or trigger the start of monitoring/P&L polling.
2. Identify whether this is:
   - A cron schedule string (e.g. node-cron pattern like '15 9 * * 1-5')
   - A time-comparison constant (e.g. dayjs().hour(9).minute(15))
   - An env-configurable value (check .env.example and Zod env schema in 
     src/schemas/ for something like MONITORING_START_TIME or similar)
3. Update ONLY the monitoring/polling start time to 09:30 IST. Do NOT change:
   - Entry window times (Wed/Fri after 09:30 AM IST — already correct, leave as is)
   - Exit window times (15:15 PM IST)
   - VIX check / daily status update time (08:40 AM IST)
   - Any other cron jobs (housekeeping/cleanup at midnight, etc.)
4. If the value is hardcoded in multiple places (e.g. duplicated across NIFTY and 
   SENSEX scheduling logic), update all occurrences consistently. Prefer 
   consolidating into a single named constant if the codebase doesn't already 
   have one, so future changes only need one edit.
5. Update any corresponding unit tests in __tests__/ that assert against the old 
   09:15 start time, so the 100% test coverage requirement (branches, functions, 
   lines, statements per jest.config.ts) still passes.
6. Run the full test suite (`pnpm test` or the CI-equivalent command) and 
   `tsc --noEmit` to confirm nothing else depends on the old time, then run 
   ESLint/Prettier checks per the existing CI workflow.
7. Show me a diff of all changed files before committing, and do not push or 
   deploy — just make the code changes and confirm tests pass locally.

Do not modify the entry, exit, VIX filter, or stoploss/profit-target percentage 
logic — this change is scoped strictly to the monitoring start time.
